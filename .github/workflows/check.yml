name: Project test & build

jobs:
  build-and-deploy:
    steps:
      - name: Checkout 🛎️
        uses: actions/checkout@v2.3.1
        with:
          persist-credentials: false

      - name: Install 🔧
        run: npm install
        with:
          secret_key: ${{ secrets.WEATHER_KEY }}
