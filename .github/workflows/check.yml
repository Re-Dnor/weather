name: check

on:
  push:
    branches:
      - main

jobs:
  build:
    steps:
      - name: Install
        run: npm install

      - name: Get key
        env:
          CI: true
          API_key: ${{ secrets.WEATHER_KEY }}
